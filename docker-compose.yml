version: "3.9"
services:
  langgraph:
    build: .
    container_name: langgraph
    env_file: .env
    environment:
      - REPO_PATH=/workspace/repo
    volumes:
      - ${HOST_REPO_PATH}:/workspace/repo
    tty: true
    stdin_open: true
